{
  "name": "browser-check",
  "version": "1.0.15",
  "type": "module",
  "scripts": {
    "check-globals": "npx playwright install chromium --with-deps && node get_globals.js"
  },
  "dependencies": {
    "playwright": "^1.42.0"
  }
}
